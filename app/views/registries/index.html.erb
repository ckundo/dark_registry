<ul>
  <% @registries.each do |registry| %>
    <li>
    <%= link_to registry.title, registry_path(registry) %>
    </li>
  <% end %>
</ul>

<strong><%= link_to "+ Create a registry", new_registry_path %></strong>
